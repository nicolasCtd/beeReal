import os 
from core.common import *
from modules import globals

def visu1(TABs):
    if globals.edited[1]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '1_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu2(TABs):
    if globals.edited[2]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '2_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu3(TABs):
    if globals.edited[3]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '3_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu4(TABs):
    if globals.edited[4]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '4_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu5(TABs):
    if globals.edited[5]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '5_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu6(TABs):
    if globals.edited[6]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '6_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu7(TABs):
    if globals.edited[7]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '7_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu8(TABs):
    if globals.edited[8]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '8_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu9(TABs):
    if globals.edited[9]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '9_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu10(TABs):
    if globals.edited[10]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '10_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu11(TABs):
    if globals.edited[11]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '11_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu12(TABs):
    if globals.edited[12]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '12_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu13(TABs):
    if globals.edited[13]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '13_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu14(TABs):
    if globals.edited[14]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '14_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu15(TABs):
    if globals.edited[15]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '15_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu16(TABs):
    if globals.edited[16]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '16_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu17(TABs):
    if globals.edited[17]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '17_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu18(TABs):
    if globals.edited[18]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '18_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu19(TABs):
    if globals.edited[19]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '19_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu20(TABs):
    if globals.edited[20]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '20_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu21(TABs):
    if globals.edited[21]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '21_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu22(TABs):
    if globals.edited[22]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '22_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu23(TABs):
    if globals.edited[23]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '23_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu24(TABs):
    if globals.edited[24]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '24_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu25(TABs):
    if globals.edited[25]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '25_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu26(TABs):
    if globals.edited[26]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '26_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu27(TABs):
    if globals.edited[27]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '27_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu28(TABs):
    if globals.edited[28]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '28_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu29(TABs):
    if globals.edited[29]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '29_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu30(TABs):
    if globals.edited[30]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '30_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu31(TABs):
    if globals.edited[31]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '31_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu32(TABs):
    if globals.edited[32]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '32_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu33(TABs):
    if globals.edited[33]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '33_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu34(TABs):
    if globals.edited[34]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '34_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu35(TABs):
    if globals.edited[35]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '35_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu36(TABs):
    if globals.edited[36]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '36_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu37(TABs):
    if globals.edited[37]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '37_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu38(TABs):
    if globals.edited[38]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '38_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu39(TABs):
    if globals.edited[39]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '39_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu40(TABs):
    if globals.edited[40]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '40_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu41(TABs):
    if globals.edited[41]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '41_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu42(TABs):
    if globals.edited[42]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '42_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu43(TABs):
    if globals.edited[43]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '43_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu44(TABs):
    if globals.edited[44]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '44_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu45(TABs):
    if globals.edited[45]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '45_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu46(TABs):
    if globals.edited[46]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '46_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu47(TABs):
    if globals.edited[47]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '47_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu48(TABs):
    if globals.edited[48]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '48_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu49(TABs):
    if globals.edited[49]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '49_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu50(TABs):
    if globals.edited[50]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '50_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu51(TABs):
    if globals.edited[51]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '51_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu52(TABs):
    if globals.edited[52]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '52_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu53(TABs):
    if globals.edited[53]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '53_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu54(TABs):
    if globals.edited[54]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '54_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu55(TABs):
    if globals.edited[55]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '55_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu56(TABs):
    if globals.edited[56]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '56_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu57(TABs):
    if globals.edited[57]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '57_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu58(TABs):
    if globals.edited[58]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '58_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu59(TABs):
    if globals.edited[59]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '59_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu60(TABs):
    if globals.edited[60]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '60_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu61(TABs):
    if globals.edited[61]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '61_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu62(TABs):
    if globals.edited[62]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '62_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu63(TABs):
    if globals.edited[63]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '63_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu64(TABs):
    if globals.edited[64]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '64_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu65(TABs):
    if globals.edited[65]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '65_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu66(TABs):
    if globals.edited[66]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '66_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu67(TABs):
    if globals.edited[67]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '67_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu68(TABs):
    if globals.edited[68]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '68_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu69(TABs):
    if globals.edited[69]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '69_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu70(TABs):
    if globals.edited[70]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '70_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu71(TABs):
    if globals.edited[71]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '71_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu72(TABs):
    if globals.edited[72]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '72_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu73(TABs):
    if globals.edited[73]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '73_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu74(TABs):
    if globals.edited[74]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '74_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu75(TABs):
    if globals.edited[75]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '75_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu76(TABs):
    if globals.edited[76]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '76_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu77(TABs):
    if globals.edited[77]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '77_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu78(TABs):
    if globals.edited[78]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '78_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu79(TABs):
    if globals.edited[79]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '79_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu80(TABs):
    if globals.edited[80]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '80_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu81(TABs):
    if globals.edited[81]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '81_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu82(TABs):
    if globals.edited[82]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '82_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu83(TABs):
    if globals.edited[83]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '83_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu84(TABs):
    if globals.edited[84]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '84_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu85(TABs):
    if globals.edited[85]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '85_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu86(TABs):
    if globals.edited[86]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '86_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu87(TABs):
    if globals.edited[87]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '87_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu88(TABs):
    if globals.edited[88]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '88_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu89(TABs):
    if globals.edited[89]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '89_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu90(TABs):
    if globals.edited[90]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '90_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu91(TABs):
    if globals.edited[91]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '91_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu92(TABs):
    if globals.edited[92]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '92_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu93(TABs):
    if globals.edited[93]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '93_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu94(TABs):
    if globals.edited[94]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '94_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu95(TABs):
    if globals.edited[95]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '95_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu96(TABs):
    if globals.edited[96]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '96_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu97(TABs):
    if globals.edited[97]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '97_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu98(TABs):
    if globals.edited[98]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '98_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu99(TABs):
    if globals.edited[99]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '99_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

def visu100(TABs):
    if globals.edited[100]:
        TABs.dialog = VISU()
        TABs.dialog.display(TABs.out + os.sep + '100_out.jpg')
    else:
        TABs.dialog = MESSAGE()
        TABs.dialog.message_erreur2()

