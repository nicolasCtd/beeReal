import os 
from core.common import *
from modules import globals

def visu1(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°1 après édition."""
    if globals.edited[1]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '1_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu2(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°2 après édition."""
    if globals.edited[2]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '2_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu3(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°3 après édition."""
    if globals.edited[3]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '3_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu4(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°4 après édition."""
    if globals.edited[4]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '4_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu5(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°5 après édition."""
    if globals.edited[5]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '5_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu6(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°6 après édition."""
    if globals.edited[6]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '6_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu7(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°7 après édition."""
    if globals.edited[7]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '7_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu8(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°8 après édition."""
    if globals.edited[8]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '8_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu9(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°9 après édition."""
    if globals.edited[9]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '9_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu10(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°10 après édition."""
    if globals.edited[10]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '10_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu11(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°11 après édition."""
    if globals.edited[11]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '11_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu12(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°12 après édition."""
    if globals.edited[12]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '12_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu13(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°13 après édition."""
    if globals.edited[13]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '13_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu14(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°14 après édition."""
    if globals.edited[14]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '14_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu15(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°15 après édition."""
    if globals.edited[15]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '15_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu16(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°16 après édition."""
    if globals.edited[16]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '16_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu17(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°17 après édition."""
    if globals.edited[17]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '17_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu18(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°18 après édition."""
    if globals.edited[18]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '18_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu19(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°19 après édition."""
    if globals.edited[19]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '19_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu20(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°20 après édition."""
    if globals.edited[20]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '20_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu21(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°21 après édition."""
    if globals.edited[21]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '21_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu22(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°22 après édition."""
    if globals.edited[22]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '22_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu23(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°23 après édition."""
    if globals.edited[23]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '23_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu24(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°24 après édition."""
    if globals.edited[24]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '24_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu25(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°25 après édition."""
    if globals.edited[25]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '25_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu26(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°26 après édition."""
    if globals.edited[26]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '26_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu27(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°27 après édition."""
    if globals.edited[27]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '27_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu28(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°28 après édition."""
    if globals.edited[28]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '28_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu29(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°29 après édition."""
    if globals.edited[29]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '29_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu30(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°30 après édition."""
    if globals.edited[30]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '30_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu31(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°31 après édition."""
    if globals.edited[31]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '31_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu32(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°32 après édition."""
    if globals.edited[32]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '32_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu33(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°33 après édition."""
    if globals.edited[33]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '33_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu34(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°34 après édition."""
    if globals.edited[34]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '34_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu35(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°35 après édition."""
    if globals.edited[35]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '35_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu36(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°36 après édition."""
    if globals.edited[36]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '36_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu37(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°37 après édition."""
    if globals.edited[37]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '37_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu38(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°38 après édition."""
    if globals.edited[38]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '38_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu39(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°39 après édition."""
    if globals.edited[39]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '39_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu40(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°40 après édition."""
    if globals.edited[40]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '40_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu41(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°41 après édition."""
    if globals.edited[41]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '41_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu42(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°42 après édition."""
    if globals.edited[42]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '42_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu43(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°43 après édition."""
    if globals.edited[43]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '43_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu44(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°44 après édition."""
    if globals.edited[44]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '44_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu45(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°45 après édition."""
    if globals.edited[45]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '45_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu46(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°46 après édition."""
    if globals.edited[46]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '46_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu47(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°47 après édition."""
    if globals.edited[47]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '47_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu48(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°48 après édition."""
    if globals.edited[48]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '48_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu49(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°49 après édition."""
    if globals.edited[49]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '49_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu50(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°50 après édition."""
    if globals.edited[50]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '50_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu51(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°51 après édition."""
    if globals.edited[51]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '51_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu52(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°52 après édition."""
    if globals.edited[52]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '52_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu53(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°53 après édition."""
    if globals.edited[53]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '53_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu54(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°54 après édition."""
    if globals.edited[54]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '54_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu55(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°55 après édition."""
    if globals.edited[55]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '55_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu56(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°56 après édition."""
    if globals.edited[56]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '56_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu57(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°57 après édition."""
    if globals.edited[57]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '57_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu58(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°58 après édition."""
    if globals.edited[58]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '58_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu59(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°59 après édition."""
    if globals.edited[59]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '59_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu60(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°60 après édition."""
    if globals.edited[60]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '60_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu61(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°61 après édition."""
    if globals.edited[61]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '61_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu62(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°62 après édition."""
    if globals.edited[62]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '62_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu63(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°63 après édition."""
    if globals.edited[63]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '63_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu64(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°64 après édition."""
    if globals.edited[64]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '64_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu65(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°65 après édition."""
    if globals.edited[65]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '65_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu66(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°66 après édition."""
    if globals.edited[66]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '66_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu67(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°67 après édition."""
    if globals.edited[67]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '67_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu68(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°68 après édition."""
    if globals.edited[68]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '68_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu69(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°69 après édition."""
    if globals.edited[69]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '69_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu70(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°70 après édition."""
    if globals.edited[70]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '70_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu71(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°71 après édition."""
    if globals.edited[71]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '71_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu72(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°72 après édition."""
    if globals.edited[72]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '72_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu73(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°73 après édition."""
    if globals.edited[73]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '73_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu74(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°74 après édition."""
    if globals.edited[74]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '74_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu75(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°75 après édition."""
    if globals.edited[75]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '75_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu76(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°76 après édition."""
    if globals.edited[76]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '76_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu77(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°77 après édition."""
    if globals.edited[77]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '77_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu78(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°78 après édition."""
    if globals.edited[78]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '78_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu79(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°79 après édition."""
    if globals.edited[79]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '79_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu80(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°80 après édition."""
    if globals.edited[80]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '80_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu81(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°81 après édition."""
    if globals.edited[81]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '81_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu82(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°82 après édition."""
    if globals.edited[82]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '82_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu83(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°83 après édition."""
    if globals.edited[83]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '83_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu84(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°84 après édition."""
    if globals.edited[84]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '84_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu85(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°85 après édition."""
    if globals.edited[85]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '85_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu86(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°86 après édition."""
    if globals.edited[86]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '86_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu87(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°87 après édition."""
    if globals.edited[87]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '87_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu88(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°88 après édition."""
    if globals.edited[88]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '88_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu89(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°89 après édition."""
    if globals.edited[89]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '89_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu90(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°90 après édition."""
    if globals.edited[90]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '90_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu91(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°91 après édition."""
    if globals.edited[91]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '91_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu92(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°92 après édition."""
    if globals.edited[92]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '92_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu93(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°93 après édition."""
    if globals.edited[93]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '93_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu94(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°94 après édition."""
    if globals.edited[94]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '94_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu95(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°95 après édition."""
    if globals.edited[95]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '95_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu96(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°96 après édition."""
    if globals.edited[96]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '96_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu97(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°97 après édition."""
    if globals.edited[97]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '97_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu98(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°98 après édition."""
    if globals.edited[98]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '98_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu99(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°99 après édition."""
    if globals.edited[99]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '99_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

def visu100(all_tabs):
    """Affiche la fenêtre de visualisation de l’image n°100 après édition."""
    if globals.edited[100]:
        all_tabs.dialog = VISU()
        all_tabs.dialog.display(all_tabs.out + os.sep + '100_out.jpg')
    else:
        all_tabs.dialog = MESSAGE()
        all_tabs.dialog.message_erreur2()

