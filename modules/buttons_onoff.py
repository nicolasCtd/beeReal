from modules import globals
from modules import buttons

def onoff1(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°1."""
    if globals.enabled[1]:
        globals.enabled[1] = 0
        buttons.turnToGrayImages(all_tabs, 1)
    else:
        globals.enabled[1] = 1
        buttons.turnToRGBImages(all_tabs, 1)
    return 0

def onoff2(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°2."""
    if globals.enabled[2]:
        globals.enabled[2] = 0
        buttons.turnToGrayImages(all_tabs, 2)
    else:
        globals.enabled[2] = 1
        buttons.turnToRGBImages(all_tabs, 2)
    return 0

def onoff3(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°3."""
    if globals.enabled[3]:
        globals.enabled[3] = 0
        buttons.turnToGrayImages(all_tabs, 3)
    else:
        globals.enabled[3] = 1
        buttons.turnToRGBImages(all_tabs, 3)
    return 0

def onoff4(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°4."""
    if globals.enabled[4]:
        globals.enabled[4] = 0
        buttons.turnToGrayImages(all_tabs, 4)
    else:
        globals.enabled[4] = 1
        buttons.turnToRGBImages(all_tabs, 4)
    return 0

def onoff5(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°5."""
    if globals.enabled[5]:
        globals.enabled[5] = 0
        buttons.turnToGrayImages(all_tabs, 5)
    else:
        globals.enabled[5] = 1
        buttons.turnToRGBImages(all_tabs, 5)
    return 0

def onoff6(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°6."""
    if globals.enabled[6]:
        globals.enabled[6] = 0
        buttons.turnToGrayImages(all_tabs, 6)
    else:
        globals.enabled[6] = 1
        buttons.turnToRGBImages(all_tabs, 6)
    return 0

def onoff7(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°7."""
    if globals.enabled[7]:
        globals.enabled[7] = 0
        buttons.turnToGrayImages(all_tabs, 7)
    else:
        globals.enabled[7] = 1
        buttons.turnToRGBImages(all_tabs, 7)
    return 0

def onoff8(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°8."""
    if globals.enabled[8]:
        globals.enabled[8] = 0
        buttons.turnToGrayImages(all_tabs, 8)
    else:
        globals.enabled[8] = 1
        buttons.turnToRGBImages(all_tabs, 8)
    return 0

def onoff9(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°9."""
    if globals.enabled[9]:
        globals.enabled[9] = 0
        buttons.turnToGrayImages(all_tabs, 9)
    else:
        globals.enabled[9] = 1
        buttons.turnToRGBImages(all_tabs, 9)
    return 0

def onoff10(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°10."""
    if globals.enabled[10]:
        globals.enabled[10] = 0
        buttons.turnToGrayImages(all_tabs, 10)
    else:
        globals.enabled[10] = 1
        buttons.turnToRGBImages(all_tabs, 10)
    return 0

def onoff11(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°11."""
    if globals.enabled[11]:
        globals.enabled[11] = 0
        buttons.turnToGrayImages(all_tabs, 11)
    else:
        globals.enabled[11] = 1
        buttons.turnToRGBImages(all_tabs, 11)
    return 0

def onoff12(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°12."""
    if globals.enabled[12]:
        globals.enabled[12] = 0
        buttons.turnToGrayImages(all_tabs, 12)
    else:
        globals.enabled[12] = 1
        buttons.turnToRGBImages(all_tabs, 12)
    return 0

def onoff13(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°13."""
    if globals.enabled[13]:
        globals.enabled[13] = 0
        buttons.turnToGrayImages(all_tabs, 13)
    else:
        globals.enabled[13] = 1
        buttons.turnToRGBImages(all_tabs, 13)
    return 0

def onoff14(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°14."""
    if globals.enabled[14]:
        globals.enabled[14] = 0
        buttons.turnToGrayImages(all_tabs, 14)
    else:
        globals.enabled[14] = 1
        buttons.turnToRGBImages(all_tabs, 14)
    return 0

def onoff15(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°15."""
    if globals.enabled[15]:
        globals.enabled[15] = 0
        buttons.turnToGrayImages(all_tabs, 15)
    else:
        globals.enabled[15] = 1
        buttons.turnToRGBImages(all_tabs, 15)
    return 0

def onoff16(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°16."""
    if globals.enabled[16]:
        globals.enabled[16] = 0
        buttons.turnToGrayImages(all_tabs, 16)
    else:
        globals.enabled[16] = 1
        buttons.turnToRGBImages(all_tabs, 16)
    return 0

def onoff17(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°17."""
    if globals.enabled[17]:
        globals.enabled[17] = 0
        buttons.turnToGrayImages(all_tabs, 17)
    else:
        globals.enabled[17] = 1
        buttons.turnToRGBImages(all_tabs, 17)
    return 0

def onoff18(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°18."""
    if globals.enabled[18]:
        globals.enabled[18] = 0
        buttons.turnToGrayImages(all_tabs, 18)
    else:
        globals.enabled[18] = 1
        buttons.turnToRGBImages(all_tabs, 18)
    return 0

def onoff19(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°19."""
    if globals.enabled[19]:
        globals.enabled[19] = 0
        buttons.turnToGrayImages(all_tabs, 19)
    else:
        globals.enabled[19] = 1
        buttons.turnToRGBImages(all_tabs, 19)
    return 0

def onoff20(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°20."""
    if globals.enabled[20]:
        globals.enabled[20] = 0
        buttons.turnToGrayImages(all_tabs, 20)
    else:
        globals.enabled[20] = 1
        buttons.turnToRGBImages(all_tabs, 20)
    return 0

def onoff21(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°21."""
    if globals.enabled[21]:
        globals.enabled[21] = 0
        buttons.turnToGrayImages(all_tabs, 21)
    else:
        globals.enabled[21] = 1
        buttons.turnToRGBImages(all_tabs, 21)
    return 0

def onoff22(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°22."""
    if globals.enabled[22]:
        globals.enabled[22] = 0
        buttons.turnToGrayImages(all_tabs, 22)
    else:
        globals.enabled[22] = 1
        buttons.turnToRGBImages(all_tabs, 22)
    return 0

def onoff23(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°23."""
    if globals.enabled[23]:
        globals.enabled[23] = 0
        buttons.turnToGrayImages(all_tabs, 23)
    else:
        globals.enabled[23] = 1
        buttons.turnToRGBImages(all_tabs, 23)
    return 0

def onoff24(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°24."""
    if globals.enabled[24]:
        globals.enabled[24] = 0
        buttons.turnToGrayImages(all_tabs, 24)
    else:
        globals.enabled[24] = 1
        buttons.turnToRGBImages(all_tabs, 24)
    return 0

def onoff25(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°25."""
    if globals.enabled[25]:
        globals.enabled[25] = 0
        buttons.turnToGrayImages(all_tabs, 25)
    else:
        globals.enabled[25] = 1
        buttons.turnToRGBImages(all_tabs, 25)
    return 0

def onoff26(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°26."""
    if globals.enabled[26]:
        globals.enabled[26] = 0
        buttons.turnToGrayImages(all_tabs, 26)
    else:
        globals.enabled[26] = 1
        buttons.turnToRGBImages(all_tabs, 26)
    return 0

def onoff27(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°27."""
    if globals.enabled[27]:
        globals.enabled[27] = 0
        buttons.turnToGrayImages(all_tabs, 27)
    else:
        globals.enabled[27] = 1
        buttons.turnToRGBImages(all_tabs, 27)
    return 0

def onoff28(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°28."""
    if globals.enabled[28]:
        globals.enabled[28] = 0
        buttons.turnToGrayImages(all_tabs, 28)
    else:
        globals.enabled[28] = 1
        buttons.turnToRGBImages(all_tabs, 28)
    return 0

def onoff29(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°29."""
    if globals.enabled[29]:
        globals.enabled[29] = 0
        buttons.turnToGrayImages(all_tabs, 29)
    else:
        globals.enabled[29] = 1
        buttons.turnToRGBImages(all_tabs, 29)
    return 0

def onoff30(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°30."""
    if globals.enabled[30]:
        globals.enabled[30] = 0
        buttons.turnToGrayImages(all_tabs, 30)
    else:
        globals.enabled[30] = 1
        buttons.turnToRGBImages(all_tabs, 30)
    return 0

def onoff31(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°31."""
    if globals.enabled[31]:
        globals.enabled[31] = 0
        buttons.turnToGrayImages(all_tabs, 31)
    else:
        globals.enabled[31] = 1
        buttons.turnToRGBImages(all_tabs, 31)
    return 0

def onoff32(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°32."""
    if globals.enabled[32]:
        globals.enabled[32] = 0
        buttons.turnToGrayImages(all_tabs, 32)
    else:
        globals.enabled[32] = 1
        buttons.turnToRGBImages(all_tabs, 32)
    return 0

def onoff33(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°33."""
    if globals.enabled[33]:
        globals.enabled[33] = 0
        buttons.turnToGrayImages(all_tabs, 33)
    else:
        globals.enabled[33] = 1
        buttons.turnToRGBImages(all_tabs, 33)
    return 0

def onoff34(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°34."""
    if globals.enabled[34]:
        globals.enabled[34] = 0
        buttons.turnToGrayImages(all_tabs, 34)
    else:
        globals.enabled[34] = 1
        buttons.turnToRGBImages(all_tabs, 34)
    return 0

def onoff35(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°35."""
    if globals.enabled[35]:
        globals.enabled[35] = 0
        buttons.turnToGrayImages(all_tabs, 35)
    else:
        globals.enabled[35] = 1
        buttons.turnToRGBImages(all_tabs, 35)
    return 0

def onoff36(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°36."""
    if globals.enabled[36]:
        globals.enabled[36] = 0
        buttons.turnToGrayImages(all_tabs, 36)
    else:
        globals.enabled[36] = 1
        buttons.turnToRGBImages(all_tabs, 36)
    return 0

def onoff37(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°37."""
    if globals.enabled[37]:
        globals.enabled[37] = 0
        buttons.turnToGrayImages(all_tabs, 37)
    else:
        globals.enabled[37] = 1
        buttons.turnToRGBImages(all_tabs, 37)
    return 0

def onoff38(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°38."""
    if globals.enabled[38]:
        globals.enabled[38] = 0
        buttons.turnToGrayImages(all_tabs, 38)
    else:
        globals.enabled[38] = 1
        buttons.turnToRGBImages(all_tabs, 38)
    return 0

def onoff39(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°39."""
    if globals.enabled[39]:
        globals.enabled[39] = 0
        buttons.turnToGrayImages(all_tabs, 39)
    else:
        globals.enabled[39] = 1
        buttons.turnToRGBImages(all_tabs, 39)
    return 0

def onoff40(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°40."""
    if globals.enabled[40]:
        globals.enabled[40] = 0
        buttons.turnToGrayImages(all_tabs, 40)
    else:
        globals.enabled[40] = 1
        buttons.turnToRGBImages(all_tabs, 40)
    return 0

def onoff41(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°41."""
    if globals.enabled[41]:
        globals.enabled[41] = 0
        buttons.turnToGrayImages(all_tabs, 41)
    else:
        globals.enabled[41] = 1
        buttons.turnToRGBImages(all_tabs, 41)
    return 0

def onoff42(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°42."""
    if globals.enabled[42]:
        globals.enabled[42] = 0
        buttons.turnToGrayImages(all_tabs, 42)
    else:
        globals.enabled[42] = 1
        buttons.turnToRGBImages(all_tabs, 42)
    return 0

def onoff43(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°43."""
    if globals.enabled[43]:
        globals.enabled[43] = 0
        buttons.turnToGrayImages(all_tabs, 43)
    else:
        globals.enabled[43] = 1
        buttons.turnToRGBImages(all_tabs, 43)
    return 0

def onoff44(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°44."""
    if globals.enabled[44]:
        globals.enabled[44] = 0
        buttons.turnToGrayImages(all_tabs, 44)
    else:
        globals.enabled[44] = 1
        buttons.turnToRGBImages(all_tabs, 44)
    return 0

def onoff45(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°45."""
    if globals.enabled[45]:
        globals.enabled[45] = 0
        buttons.turnToGrayImages(all_tabs, 45)
    else:
        globals.enabled[45] = 1
        buttons.turnToRGBImages(all_tabs, 45)
    return 0

def onoff46(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°46."""
    if globals.enabled[46]:
        globals.enabled[46] = 0
        buttons.turnToGrayImages(all_tabs, 46)
    else:
        globals.enabled[46] = 1
        buttons.turnToRGBImages(all_tabs, 46)
    return 0

def onoff47(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°47."""
    if globals.enabled[47]:
        globals.enabled[47] = 0
        buttons.turnToGrayImages(all_tabs, 47)
    else:
        globals.enabled[47] = 1
        buttons.turnToRGBImages(all_tabs, 47)
    return 0

def onoff48(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°48."""
    if globals.enabled[48]:
        globals.enabled[48] = 0
        buttons.turnToGrayImages(all_tabs, 48)
    else:
        globals.enabled[48] = 1
        buttons.turnToRGBImages(all_tabs, 48)
    return 0

def onoff49(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°49."""
    if globals.enabled[49]:
        globals.enabled[49] = 0
        buttons.turnToGrayImages(all_tabs, 49)
    else:
        globals.enabled[49] = 1
        buttons.turnToRGBImages(all_tabs, 49)
    return 0

def onoff50(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°50."""
    if globals.enabled[50]:
        globals.enabled[50] = 0
        buttons.turnToGrayImages(all_tabs, 50)
    else:
        globals.enabled[50] = 1
        buttons.turnToRGBImages(all_tabs, 50)
    return 0

def onoff51(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°51."""
    if globals.enabled[51]:
        globals.enabled[51] = 0
        buttons.turnToGrayImages(all_tabs, 51)
    else:
        globals.enabled[51] = 1
        buttons.turnToRGBImages(all_tabs, 51)
    return 0

def onoff52(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°52."""
    if globals.enabled[52]:
        globals.enabled[52] = 0
        buttons.turnToGrayImages(all_tabs, 52)
    else:
        globals.enabled[52] = 1
        buttons.turnToRGBImages(all_tabs, 52)
    return 0

def onoff53(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°53."""
    if globals.enabled[53]:
        globals.enabled[53] = 0
        buttons.turnToGrayImages(all_tabs, 53)
    else:
        globals.enabled[53] = 1
        buttons.turnToRGBImages(all_tabs, 53)
    return 0

def onoff54(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°54."""
    if globals.enabled[54]:
        globals.enabled[54] = 0
        buttons.turnToGrayImages(all_tabs, 54)
    else:
        globals.enabled[54] = 1
        buttons.turnToRGBImages(all_tabs, 54)
    return 0

def onoff55(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°55."""
    if globals.enabled[55]:
        globals.enabled[55] = 0
        buttons.turnToGrayImages(all_tabs, 55)
    else:
        globals.enabled[55] = 1
        buttons.turnToRGBImages(all_tabs, 55)
    return 0

def onoff56(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°56."""
    if globals.enabled[56]:
        globals.enabled[56] = 0
        buttons.turnToGrayImages(all_tabs, 56)
    else:
        globals.enabled[56] = 1
        buttons.turnToRGBImages(all_tabs, 56)
    return 0

def onoff57(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°57."""
    if globals.enabled[57]:
        globals.enabled[57] = 0
        buttons.turnToGrayImages(all_tabs, 57)
    else:
        globals.enabled[57] = 1
        buttons.turnToRGBImages(all_tabs, 57)
    return 0

def onoff58(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°58."""
    if globals.enabled[58]:
        globals.enabled[58] = 0
        buttons.turnToGrayImages(all_tabs, 58)
    else:
        globals.enabled[58] = 1
        buttons.turnToRGBImages(all_tabs, 58)
    return 0

def onoff59(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°59."""
    if globals.enabled[59]:
        globals.enabled[59] = 0
        buttons.turnToGrayImages(all_tabs, 59)
    else:
        globals.enabled[59] = 1
        buttons.turnToRGBImages(all_tabs, 59)
    return 0

def onoff60(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°60."""
    if globals.enabled[60]:
        globals.enabled[60] = 0
        buttons.turnToGrayImages(all_tabs, 60)
    else:
        globals.enabled[60] = 1
        buttons.turnToRGBImages(all_tabs, 60)
    return 0

def onoff61(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°61."""
    if globals.enabled[61]:
        globals.enabled[61] = 0
        buttons.turnToGrayImages(all_tabs, 61)
    else:
        globals.enabled[61] = 1
        buttons.turnToRGBImages(all_tabs, 61)
    return 0

def onoff62(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°62."""
    if globals.enabled[62]:
        globals.enabled[62] = 0
        buttons.turnToGrayImages(all_tabs, 62)
    else:
        globals.enabled[62] = 1
        buttons.turnToRGBImages(all_tabs, 62)
    return 0

def onoff63(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°63."""
    if globals.enabled[63]:
        globals.enabled[63] = 0
        buttons.turnToGrayImages(all_tabs, 63)
    else:
        globals.enabled[63] = 1
        buttons.turnToRGBImages(all_tabs, 63)
    return 0

def onoff64(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°64."""
    if globals.enabled[64]:
        globals.enabled[64] = 0
        buttons.turnToGrayImages(all_tabs, 64)
    else:
        globals.enabled[64] = 1
        buttons.turnToRGBImages(all_tabs, 64)
    return 0

def onoff65(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°65."""
    if globals.enabled[65]:
        globals.enabled[65] = 0
        buttons.turnToGrayImages(all_tabs, 65)
    else:
        globals.enabled[65] = 1
        buttons.turnToRGBImages(all_tabs, 65)
    return 0

def onoff66(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°66."""
    if globals.enabled[66]:
        globals.enabled[66] = 0
        buttons.turnToGrayImages(all_tabs, 66)
    else:
        globals.enabled[66] = 1
        buttons.turnToRGBImages(all_tabs, 66)
    return 0

def onoff67(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°67."""
    if globals.enabled[67]:
        globals.enabled[67] = 0
        buttons.turnToGrayImages(all_tabs, 67)
    else:
        globals.enabled[67] = 1
        buttons.turnToRGBImages(all_tabs, 67)
    return 0

def onoff68(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°68."""
    if globals.enabled[68]:
        globals.enabled[68] = 0
        buttons.turnToGrayImages(all_tabs, 68)
    else:
        globals.enabled[68] = 1
        buttons.turnToRGBImages(all_tabs, 68)
    return 0

def onoff69(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°69."""
    if globals.enabled[69]:
        globals.enabled[69] = 0
        buttons.turnToGrayImages(all_tabs, 69)
    else:
        globals.enabled[69] = 1
        buttons.turnToRGBImages(all_tabs, 69)
    return 0

def onoff70(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°70."""
    if globals.enabled[70]:
        globals.enabled[70] = 0
        buttons.turnToGrayImages(all_tabs, 70)
    else:
        globals.enabled[70] = 1
        buttons.turnToRGBImages(all_tabs, 70)
    return 0

def onoff71(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°71."""
    if globals.enabled[71]:
        globals.enabled[71] = 0
        buttons.turnToGrayImages(all_tabs, 71)
    else:
        globals.enabled[71] = 1
        buttons.turnToRGBImages(all_tabs, 71)
    return 0

def onoff72(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°72."""
    if globals.enabled[72]:
        globals.enabled[72] = 0
        buttons.turnToGrayImages(all_tabs, 72)
    else:
        globals.enabled[72] = 1
        buttons.turnToRGBImages(all_tabs, 72)
    return 0

def onoff73(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°73."""
    if globals.enabled[73]:
        globals.enabled[73] = 0
        buttons.turnToGrayImages(all_tabs, 73)
    else:
        globals.enabled[73] = 1
        buttons.turnToRGBImages(all_tabs, 73)
    return 0

def onoff74(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°74."""
    if globals.enabled[74]:
        globals.enabled[74] = 0
        buttons.turnToGrayImages(all_tabs, 74)
    else:
        globals.enabled[74] = 1
        buttons.turnToRGBImages(all_tabs, 74)
    return 0

def onoff75(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°75."""
    if globals.enabled[75]:
        globals.enabled[75] = 0
        buttons.turnToGrayImages(all_tabs, 75)
    else:
        globals.enabled[75] = 1
        buttons.turnToRGBImages(all_tabs, 75)
    return 0

def onoff76(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°76."""
    if globals.enabled[76]:
        globals.enabled[76] = 0
        buttons.turnToGrayImages(all_tabs, 76)
    else:
        globals.enabled[76] = 1
        buttons.turnToRGBImages(all_tabs, 76)
    return 0

def onoff77(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°77."""
    if globals.enabled[77]:
        globals.enabled[77] = 0
        buttons.turnToGrayImages(all_tabs, 77)
    else:
        globals.enabled[77] = 1
        buttons.turnToRGBImages(all_tabs, 77)
    return 0

def onoff78(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°78."""
    if globals.enabled[78]:
        globals.enabled[78] = 0
        buttons.turnToGrayImages(all_tabs, 78)
    else:
        globals.enabled[78] = 1
        buttons.turnToRGBImages(all_tabs, 78)
    return 0

def onoff79(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°79."""
    if globals.enabled[79]:
        globals.enabled[79] = 0
        buttons.turnToGrayImages(all_tabs, 79)
    else:
        globals.enabled[79] = 1
        buttons.turnToRGBImages(all_tabs, 79)
    return 0

def onoff80(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°80."""
    if globals.enabled[80]:
        globals.enabled[80] = 0
        buttons.turnToGrayImages(all_tabs, 80)
    else:
        globals.enabled[80] = 1
        buttons.turnToRGBImages(all_tabs, 80)
    return 0

def onoff81(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°81."""
    if globals.enabled[81]:
        globals.enabled[81] = 0
        buttons.turnToGrayImages(all_tabs, 81)
    else:
        globals.enabled[81] = 1
        buttons.turnToRGBImages(all_tabs, 81)
    return 0

def onoff82(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°82."""
    if globals.enabled[82]:
        globals.enabled[82] = 0
        buttons.turnToGrayImages(all_tabs, 82)
    else:
        globals.enabled[82] = 1
        buttons.turnToRGBImages(all_tabs, 82)
    return 0

def onoff83(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°83."""
    if globals.enabled[83]:
        globals.enabled[83] = 0
        buttons.turnToGrayImages(all_tabs, 83)
    else:
        globals.enabled[83] = 1
        buttons.turnToRGBImages(all_tabs, 83)
    return 0

def onoff84(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°84."""
    if globals.enabled[84]:
        globals.enabled[84] = 0
        buttons.turnToGrayImages(all_tabs, 84)
    else:
        globals.enabled[84] = 1
        buttons.turnToRGBImages(all_tabs, 84)
    return 0

def onoff85(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°85."""
    if globals.enabled[85]:
        globals.enabled[85] = 0
        buttons.turnToGrayImages(all_tabs, 85)
    else:
        globals.enabled[85] = 1
        buttons.turnToRGBImages(all_tabs, 85)
    return 0

def onoff86(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°86."""
    if globals.enabled[86]:
        globals.enabled[86] = 0
        buttons.turnToGrayImages(all_tabs, 86)
    else:
        globals.enabled[86] = 1
        buttons.turnToRGBImages(all_tabs, 86)
    return 0

def onoff87(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°87."""
    if globals.enabled[87]:
        globals.enabled[87] = 0
        buttons.turnToGrayImages(all_tabs, 87)
    else:
        globals.enabled[87] = 1
        buttons.turnToRGBImages(all_tabs, 87)
    return 0

def onoff88(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°88."""
    if globals.enabled[88]:
        globals.enabled[88] = 0
        buttons.turnToGrayImages(all_tabs, 88)
    else:
        globals.enabled[88] = 1
        buttons.turnToRGBImages(all_tabs, 88)
    return 0

def onoff89(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°89."""
    if globals.enabled[89]:
        globals.enabled[89] = 0
        buttons.turnToGrayImages(all_tabs, 89)
    else:
        globals.enabled[89] = 1
        buttons.turnToRGBImages(all_tabs, 89)
    return 0

def onoff90(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°90."""
    if globals.enabled[90]:
        globals.enabled[90] = 0
        buttons.turnToGrayImages(all_tabs, 90)
    else:
        globals.enabled[90] = 1
        buttons.turnToRGBImages(all_tabs, 90)
    return 0

def onoff91(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°91."""
    if globals.enabled[91]:
        globals.enabled[91] = 0
        buttons.turnToGrayImages(all_tabs, 91)
    else:
        globals.enabled[91] = 1
        buttons.turnToRGBImages(all_tabs, 91)
    return 0

def onoff92(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°92."""
    if globals.enabled[92]:
        globals.enabled[92] = 0
        buttons.turnToGrayImages(all_tabs, 92)
    else:
        globals.enabled[92] = 1
        buttons.turnToRGBImages(all_tabs, 92)
    return 0

def onoff93(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°93."""
    if globals.enabled[93]:
        globals.enabled[93] = 0
        buttons.turnToGrayImages(all_tabs, 93)
    else:
        globals.enabled[93] = 1
        buttons.turnToRGBImages(all_tabs, 93)
    return 0

def onoff94(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°94."""
    if globals.enabled[94]:
        globals.enabled[94] = 0
        buttons.turnToGrayImages(all_tabs, 94)
    else:
        globals.enabled[94] = 1
        buttons.turnToRGBImages(all_tabs, 94)
    return 0

def onoff95(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°95."""
    if globals.enabled[95]:
        globals.enabled[95] = 0
        buttons.turnToGrayImages(all_tabs, 95)
    else:
        globals.enabled[95] = 1
        buttons.turnToRGBImages(all_tabs, 95)
    return 0

def onoff96(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°96."""
    if globals.enabled[96]:
        globals.enabled[96] = 0
        buttons.turnToGrayImages(all_tabs, 96)
    else:
        globals.enabled[96] = 1
        buttons.turnToRGBImages(all_tabs, 96)
    return 0

def onoff97(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°97."""
    if globals.enabled[97]:
        globals.enabled[97] = 0
        buttons.turnToGrayImages(all_tabs, 97)
    else:
        globals.enabled[97] = 1
        buttons.turnToRGBImages(all_tabs, 97)
    return 0

def onoff98(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°98."""
    if globals.enabled[98]:
        globals.enabled[98] = 0
        buttons.turnToGrayImages(all_tabs, 98)
    else:
        globals.enabled[98] = 1
        buttons.turnToRGBImages(all_tabs, 98)
    return 0

def onoff99(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°99."""
    if globals.enabled[99]:
        globals.enabled[99] = 0
        buttons.turnToGrayImages(all_tabs, 99)
    else:
        globals.enabled[99] = 1
        buttons.turnToRGBImages(all_tabs, 99)
    return 0

def onoff100(all_tabs):
    """La fonction permet d’activer/désactiver l’image n°100."""
    if globals.enabled[100]:
        globals.enabled[100] = 0
        buttons.turnToGrayImages(all_tabs, 100)
    else:
        globals.enabled[100] = 1
        buttons.turnToRGBImages(all_tabs, 100)
    return 0

