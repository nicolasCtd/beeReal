from modules import globals
from modules import buttons

def onoff1(TABs):
    if globals.enabled[1-1]:
        globals.enabled[1-1] = 0
        buttons.turnToGrayImages(TABs, 1)
    else:
        globals.enabled[1-1] = 1
        buttons.turnToRGBImages(TABs, 1)
    return 0

def onoff2(TABs):
    if globals.enabled[2-1]:
        globals.enabled[2-1] = 0
        buttons.turnToGrayImages(TABs, 2)
    else:
        globals.enabled[2-1] = 1
        buttons.turnToRGBImages(TABs, 2)
    return 0

def onoff3(TABs):
    if globals.enabled[3-1]:
        globals.enabled[3-1] = 0
        buttons.turnToGrayImages(TABs, 3)
    else:
        globals.enabled[3-1] = 1
        buttons.turnToRGBImages(TABs, 3)
    return 0

def onoff4(TABs):
    if globals.enabled[4-1]:
        globals.enabled[4-1] = 0
        buttons.turnToGrayImages(TABs, 4)
    else:
        globals.enabled[4-1] = 1
        buttons.turnToRGBImages(TABs, 4)
    return 0

def onoff5(TABs):
    if globals.enabled[5-1]:
        globals.enabled[5-1] = 0
        buttons.turnToGrayImages(TABs, 5)
    else:
        globals.enabled[5-1] = 1
        buttons.turnToRGBImages(TABs, 5)
    return 0

def onoff6(TABs):
    if globals.enabled[6-1]:
        globals.enabled[6-1] = 0
        buttons.turnToGrayImages(TABs, 6)
    else:
        globals.enabled[6-1] = 1
        buttons.turnToRGBImages(TABs, 6)
    return 0

def onoff7(TABs):
    if globals.enabled[7-1]:
        globals.enabled[7-1] = 0
        buttons.turnToGrayImages(TABs, 7)
    else:
        globals.enabled[7-1] = 1
        buttons.turnToRGBImages(TABs, 7)
    return 0

def onoff8(TABs):
    if globals.enabled[8-1]:
        globals.enabled[8-1] = 0
        buttons.turnToGrayImages(TABs, 8)
    else:
        globals.enabled[8-1] = 1
        buttons.turnToRGBImages(TABs, 8)
    return 0

def onoff9(TABs):
    if globals.enabled[9-1]:
        globals.enabled[9-1] = 0
        buttons.turnToGrayImages(TABs, 9)
    else:
        globals.enabled[9-1] = 1
        buttons.turnToRGBImages(TABs, 9)
    return 0

def onoff10(TABs):
    if globals.enabled[10-1]:
        globals.enabled[10-1] = 0
        buttons.turnToGrayImages(TABs, 10)
    else:
        globals.enabled[10-1] = 1
        buttons.turnToRGBImages(TABs, 10)
    return 0

def onoff11(TABs):
    if globals.enabled[11-1]:
        globals.enabled[11-1] = 0
        buttons.turnToGrayImages(TABs, 11)
    else:
        globals.enabled[11-1] = 1
        buttons.turnToRGBImages(TABs, 11)
    return 0

def onoff12(TABs):
    if globals.enabled[12-1]:
        globals.enabled[12-1] = 0
        buttons.turnToGrayImages(TABs, 12)
    else:
        globals.enabled[12-1] = 1
        buttons.turnToRGBImages(TABs, 12)
    return 0

def onoff13(TABs):
    if globals.enabled[13-1]:
        globals.enabled[13-1] = 0
        buttons.turnToGrayImages(TABs, 13)
    else:
        globals.enabled[13-1] = 1
        buttons.turnToRGBImages(TABs, 13)
    return 0

def onoff14(TABs):
    if globals.enabled[14-1]:
        globals.enabled[14-1] = 0
        buttons.turnToGrayImages(TABs, 14)
    else:
        globals.enabled[14-1] = 1
        buttons.turnToRGBImages(TABs, 14)
    return 0

def onoff15(TABs):
    if globals.enabled[15-1]:
        globals.enabled[15-1] = 0
        buttons.turnToGrayImages(TABs, 15)
    else:
        globals.enabled[15-1] = 1
        buttons.turnToRGBImages(TABs, 15)
    return 0

def onoff16(TABs):
    if globals.enabled[16-1]:
        globals.enabled[16-1] = 0
        buttons.turnToGrayImages(TABs, 16)
    else:
        globals.enabled[16-1] = 1
        buttons.turnToRGBImages(TABs, 16)
    return 0

def onoff17(TABs):
    if globals.enabled[17-1]:
        globals.enabled[17-1] = 0
        buttons.turnToGrayImages(TABs, 17)
    else:
        globals.enabled[17-1] = 1
        buttons.turnToRGBImages(TABs, 17)
    return 0

def onoff18(TABs):
    if globals.enabled[18-1]:
        globals.enabled[18-1] = 0
        buttons.turnToGrayImages(TABs, 18)
    else:
        globals.enabled[18-1] = 1
        buttons.turnToRGBImages(TABs, 18)
    return 0

def onoff19(TABs):
    if globals.enabled[19-1]:
        globals.enabled[19-1] = 0
        buttons.turnToGrayImages(TABs, 19)
    else:
        globals.enabled[19-1] = 1
        buttons.turnToRGBImages(TABs, 19)
    return 0

def onoff20(TABs):
    if globals.enabled[20-1]:
        globals.enabled[20-1] = 0
        buttons.turnToGrayImages(TABs, 20)
    else:
        globals.enabled[20-1] = 1
        buttons.turnToRGBImages(TABs, 20)
    return 0

def onoff21(TABs):
    if globals.enabled[21-1]:
        globals.enabled[21-1] = 0
        buttons.turnToGrayImages(TABs, 21)
    else:
        globals.enabled[21-1] = 1
        buttons.turnToRGBImages(TABs, 21)
    return 0

def onoff22(TABs):
    if globals.enabled[22-1]:
        globals.enabled[22-1] = 0
        buttons.turnToGrayImages(TABs, 22)
    else:
        globals.enabled[22-1] = 1
        buttons.turnToRGBImages(TABs, 22)
    return 0

def onoff23(TABs):
    if globals.enabled[23-1]:
        globals.enabled[23-1] = 0
        buttons.turnToGrayImages(TABs, 23)
    else:
        globals.enabled[23-1] = 1
        buttons.turnToRGBImages(TABs, 23)
    return 0

def onoff24(TABs):
    if globals.enabled[24-1]:
        globals.enabled[24-1] = 0
        buttons.turnToGrayImages(TABs, 24)
    else:
        globals.enabled[24-1] = 1
        buttons.turnToRGBImages(TABs, 24)
    return 0

def onoff25(TABs):
    if globals.enabled[25-1]:
        globals.enabled[25-1] = 0
        buttons.turnToGrayImages(TABs, 25)
    else:
        globals.enabled[25-1] = 1
        buttons.turnToRGBImages(TABs, 25)
    return 0

def onoff26(TABs):
    if globals.enabled[26-1]:
        globals.enabled[26-1] = 0
        buttons.turnToGrayImages(TABs, 26)
    else:
        globals.enabled[26-1] = 1
        buttons.turnToRGBImages(TABs, 26)
    return 0

def onoff27(TABs):
    if globals.enabled[27-1]:
        globals.enabled[27-1] = 0
        buttons.turnToGrayImages(TABs, 27)
    else:
        globals.enabled[27-1] = 1
        buttons.turnToRGBImages(TABs, 27)
    return 0

def onoff28(TABs):
    if globals.enabled[28-1]:
        globals.enabled[28-1] = 0
        buttons.turnToGrayImages(TABs, 28)
    else:
        globals.enabled[28-1] = 1
        buttons.turnToRGBImages(TABs, 28)
    return 0

def onoff29(TABs):
    if globals.enabled[29-1]:
        globals.enabled[29-1] = 0
        buttons.turnToGrayImages(TABs, 29)
    else:
        globals.enabled[29-1] = 1
        buttons.turnToRGBImages(TABs, 29)
    return 0

def onoff30(TABs):
    if globals.enabled[30-1]:
        globals.enabled[30-1] = 0
        buttons.turnToGrayImages(TABs, 30)
    else:
        globals.enabled[30-1] = 1
        buttons.turnToRGBImages(TABs, 30)
    return 0

def onoff31(TABs):
    if globals.enabled[31-1]:
        globals.enabled[31-1] = 0
        buttons.turnToGrayImages(TABs, 31)
    else:
        globals.enabled[31-1] = 1
        buttons.turnToRGBImages(TABs, 31)
    return 0

def onoff32(TABs):
    if globals.enabled[32-1]:
        globals.enabled[32-1] = 0
        buttons.turnToGrayImages(TABs, 32)
    else:
        globals.enabled[32-1] = 1
        buttons.turnToRGBImages(TABs, 32)
    return 0

def onoff33(TABs):
    if globals.enabled[33-1]:
        globals.enabled[33-1] = 0
        buttons.turnToGrayImages(TABs, 33)
    else:
        globals.enabled[33-1] = 1
        buttons.turnToRGBImages(TABs, 33)
    return 0

def onoff34(TABs):
    if globals.enabled[34-1]:
        globals.enabled[34-1] = 0
        buttons.turnToGrayImages(TABs, 34)
    else:
        globals.enabled[34-1] = 1
        buttons.turnToRGBImages(TABs, 34)
    return 0

def onoff35(TABs):
    if globals.enabled[35-1]:
        globals.enabled[35-1] = 0
        buttons.turnToGrayImages(TABs, 35)
    else:
        globals.enabled[35-1] = 1
        buttons.turnToRGBImages(TABs, 35)
    return 0

def onoff36(TABs):
    if globals.enabled[36-1]:
        globals.enabled[36-1] = 0
        buttons.turnToGrayImages(TABs, 36)
    else:
        globals.enabled[36-1] = 1
        buttons.turnToRGBImages(TABs, 36)
    return 0

def onoff37(TABs):
    if globals.enabled[37-1]:
        globals.enabled[37-1] = 0
        buttons.turnToGrayImages(TABs, 37)
    else:
        globals.enabled[37-1] = 1
        buttons.turnToRGBImages(TABs, 37)
    return 0

def onoff38(TABs):
    if globals.enabled[38-1]:
        globals.enabled[38-1] = 0
        buttons.turnToGrayImages(TABs, 38)
    else:
        globals.enabled[38-1] = 1
        buttons.turnToRGBImages(TABs, 38)
    return 0

def onoff39(TABs):
    if globals.enabled[39-1]:
        globals.enabled[39-1] = 0
        buttons.turnToGrayImages(TABs, 39)
    else:
        globals.enabled[39-1] = 1
        buttons.turnToRGBImages(TABs, 39)
    return 0

def onoff40(TABs):
    if globals.enabled[40-1]:
        globals.enabled[40-1] = 0
        buttons.turnToGrayImages(TABs, 40)
    else:
        globals.enabled[40-1] = 1
        buttons.turnToRGBImages(TABs, 40)
    return 0

def onoff41(TABs):
    if globals.enabled[41-1]:
        globals.enabled[41-1] = 0
        buttons.turnToGrayImages(TABs, 41)
    else:
        globals.enabled[41-1] = 1
        buttons.turnToRGBImages(TABs, 41)
    return 0

def onoff42(TABs):
    if globals.enabled[42-1]:
        globals.enabled[42-1] = 0
        buttons.turnToGrayImages(TABs, 42)
    else:
        globals.enabled[42-1] = 1
        buttons.turnToRGBImages(TABs, 42)
    return 0

def onoff43(TABs):
    if globals.enabled[43-1]:
        globals.enabled[43-1] = 0
        buttons.turnToGrayImages(TABs, 43)
    else:
        globals.enabled[43-1] = 1
        buttons.turnToRGBImages(TABs, 43)
    return 0

def onoff44(TABs):
    if globals.enabled[44-1]:
        globals.enabled[44-1] = 0
        buttons.turnToGrayImages(TABs, 44)
    else:
        globals.enabled[44-1] = 1
        buttons.turnToRGBImages(TABs, 44)
    return 0

def onoff45(TABs):
    if globals.enabled[45-1]:
        globals.enabled[45-1] = 0
        buttons.turnToGrayImages(TABs, 45)
    else:
        globals.enabled[45-1] = 1
        buttons.turnToRGBImages(TABs, 45)
    return 0

def onoff46(TABs):
    if globals.enabled[46-1]:
        globals.enabled[46-1] = 0
        buttons.turnToGrayImages(TABs, 46)
    else:
        globals.enabled[46-1] = 1
        buttons.turnToRGBImages(TABs, 46)
    return 0

def onoff47(TABs):
    if globals.enabled[47-1]:
        globals.enabled[47-1] = 0
        buttons.turnToGrayImages(TABs, 47)
    else:
        globals.enabled[47-1] = 1
        buttons.turnToRGBImages(TABs, 47)
    return 0

def onoff48(TABs):
    if globals.enabled[48-1]:
        globals.enabled[48-1] = 0
        buttons.turnToGrayImages(TABs, 48)
    else:
        globals.enabled[48-1] = 1
        buttons.turnToRGBImages(TABs, 48)
    return 0

def onoff49(TABs):
    if globals.enabled[49-1]:
        globals.enabled[49-1] = 0
        buttons.turnToGrayImages(TABs, 49)
    else:
        globals.enabled[49-1] = 1
        buttons.turnToRGBImages(TABs, 49)
    return 0

def onoff50(TABs):
    if globals.enabled[50-1]:
        globals.enabled[50-1] = 0
        buttons.turnToGrayImages(TABs, 50)
    else:
        globals.enabled[50-1] = 1
        buttons.turnToRGBImages(TABs, 50)
    return 0

def onoff51(TABs):
    if globals.enabled[51-1]:
        globals.enabled[51-1] = 0
        buttons.turnToGrayImages(TABs, 51)
    else:
        globals.enabled[51-1] = 1
        buttons.turnToRGBImages(TABs, 51)
    return 0

def onoff52(TABs):
    if globals.enabled[52-1]:
        globals.enabled[52-1] = 0
        buttons.turnToGrayImages(TABs, 52)
    else:
        globals.enabled[52-1] = 1
        buttons.turnToRGBImages(TABs, 52)
    return 0

def onoff53(TABs):
    if globals.enabled[53-1]:
        globals.enabled[53-1] = 0
        buttons.turnToGrayImages(TABs, 53)
    else:
        globals.enabled[53-1] = 1
        buttons.turnToRGBImages(TABs, 53)
    return 0

def onoff54(TABs):
    if globals.enabled[54-1]:
        globals.enabled[54-1] = 0
        buttons.turnToGrayImages(TABs, 54)
    else:
        globals.enabled[54-1] = 1
        buttons.turnToRGBImages(TABs, 54)
    return 0

def onoff55(TABs):
    if globals.enabled[55-1]:
        globals.enabled[55-1] = 0
        buttons.turnToGrayImages(TABs, 55)
    else:
        globals.enabled[55-1] = 1
        buttons.turnToRGBImages(TABs, 55)
    return 0

def onoff56(TABs):
    if globals.enabled[56-1]:
        globals.enabled[56-1] = 0
        buttons.turnToGrayImages(TABs, 56)
    else:
        globals.enabled[56-1] = 1
        buttons.turnToRGBImages(TABs, 56)
    return 0

def onoff57(TABs):
    if globals.enabled[57-1]:
        globals.enabled[57-1] = 0
        buttons.turnToGrayImages(TABs, 57)
    else:
        globals.enabled[57-1] = 1
        buttons.turnToRGBImages(TABs, 57)
    return 0

def onoff58(TABs):
    if globals.enabled[58-1]:
        globals.enabled[58-1] = 0
        buttons.turnToGrayImages(TABs, 58)
    else:
        globals.enabled[58-1] = 1
        buttons.turnToRGBImages(TABs, 58)
    return 0

def onoff59(TABs):
    if globals.enabled[59-1]:
        globals.enabled[59-1] = 0
        buttons.turnToGrayImages(TABs, 59)
    else:
        globals.enabled[59-1] = 1
        buttons.turnToRGBImages(TABs, 59)
    return 0

def onoff60(TABs):
    if globals.enabled[60-1]:
        globals.enabled[60-1] = 0
        buttons.turnToGrayImages(TABs, 60)
    else:
        globals.enabled[60-1] = 1
        buttons.turnToRGBImages(TABs, 60)
    return 0

def onoff61(TABs):
    if globals.enabled[61-1]:
        globals.enabled[61-1] = 0
        buttons.turnToGrayImages(TABs, 61)
    else:
        globals.enabled[61-1] = 1
        buttons.turnToRGBImages(TABs, 61)
    return 0

def onoff62(TABs):
    if globals.enabled[62-1]:
        globals.enabled[62-1] = 0
        buttons.turnToGrayImages(TABs, 62)
    else:
        globals.enabled[62-1] = 1
        buttons.turnToRGBImages(TABs, 62)
    return 0

def onoff63(TABs):
    if globals.enabled[63-1]:
        globals.enabled[63-1] = 0
        buttons.turnToGrayImages(TABs, 63)
    else:
        globals.enabled[63-1] = 1
        buttons.turnToRGBImages(TABs, 63)
    return 0

def onoff64(TABs):
    if globals.enabled[64-1]:
        globals.enabled[64-1] = 0
        buttons.turnToGrayImages(TABs, 64)
    else:
        globals.enabled[64-1] = 1
        buttons.turnToRGBImages(TABs, 64)
    return 0

def onoff65(TABs):
    if globals.enabled[65-1]:
        globals.enabled[65-1] = 0
        buttons.turnToGrayImages(TABs, 65)
    else:
        globals.enabled[65-1] = 1
        buttons.turnToRGBImages(TABs, 65)
    return 0

def onoff66(TABs):
    if globals.enabled[66-1]:
        globals.enabled[66-1] = 0
        buttons.turnToGrayImages(TABs, 66)
    else:
        globals.enabled[66-1] = 1
        buttons.turnToRGBImages(TABs, 66)
    return 0

def onoff67(TABs):
    if globals.enabled[67-1]:
        globals.enabled[67-1] = 0
        buttons.turnToGrayImages(TABs, 67)
    else:
        globals.enabled[67-1] = 1
        buttons.turnToRGBImages(TABs, 67)
    return 0

def onoff68(TABs):
    if globals.enabled[68-1]:
        globals.enabled[68-1] = 0
        buttons.turnToGrayImages(TABs, 68)
    else:
        globals.enabled[68-1] = 1
        buttons.turnToRGBImages(TABs, 68)
    return 0

def onoff69(TABs):
    if globals.enabled[69-1]:
        globals.enabled[69-1] = 0
        buttons.turnToGrayImages(TABs, 69)
    else:
        globals.enabled[69-1] = 1
        buttons.turnToRGBImages(TABs, 69)
    return 0

def onoff70(TABs):
    if globals.enabled[70-1]:
        globals.enabled[70-1] = 0
        buttons.turnToGrayImages(TABs, 70)
    else:
        globals.enabled[70-1] = 1
        buttons.turnToRGBImages(TABs, 70)
    return 0

def onoff71(TABs):
    if globals.enabled[71-1]:
        globals.enabled[71-1] = 0
        buttons.turnToGrayImages(TABs, 71)
    else:
        globals.enabled[71-1] = 1
        buttons.turnToRGBImages(TABs, 71)
    return 0

def onoff72(TABs):
    if globals.enabled[72-1]:
        globals.enabled[72-1] = 0
        buttons.turnToGrayImages(TABs, 72)
    else:
        globals.enabled[72-1] = 1
        buttons.turnToRGBImages(TABs, 72)
    return 0

def onoff73(TABs):
    if globals.enabled[73-1]:
        globals.enabled[73-1] = 0
        buttons.turnToGrayImages(TABs, 73)
    else:
        globals.enabled[73-1] = 1
        buttons.turnToRGBImages(TABs, 73)
    return 0

def onoff74(TABs):
    if globals.enabled[74-1]:
        globals.enabled[74-1] = 0
        buttons.turnToGrayImages(TABs, 74)
    else:
        globals.enabled[74-1] = 1
        buttons.turnToRGBImages(TABs, 74)
    return 0

def onoff75(TABs):
    if globals.enabled[75-1]:
        globals.enabled[75-1] = 0
        buttons.turnToGrayImages(TABs, 75)
    else:
        globals.enabled[75-1] = 1
        buttons.turnToRGBImages(TABs, 75)
    return 0

def onoff76(TABs):
    if globals.enabled[76-1]:
        globals.enabled[76-1] = 0
        buttons.turnToGrayImages(TABs, 76)
    else:
        globals.enabled[76-1] = 1
        buttons.turnToRGBImages(TABs, 76)
    return 0

def onoff77(TABs):
    if globals.enabled[77-1]:
        globals.enabled[77-1] = 0
        buttons.turnToGrayImages(TABs, 77)
    else:
        globals.enabled[77-1] = 1
        buttons.turnToRGBImages(TABs, 77)
    return 0

def onoff78(TABs):
    if globals.enabled[78-1]:
        globals.enabled[78-1] = 0
        buttons.turnToGrayImages(TABs, 78)
    else:
        globals.enabled[78-1] = 1
        buttons.turnToRGBImages(TABs, 78)
    return 0

def onoff79(TABs):
    if globals.enabled[79-1]:
        globals.enabled[79-1] = 0
        buttons.turnToGrayImages(TABs, 79)
    else:
        globals.enabled[79-1] = 1
        buttons.turnToRGBImages(TABs, 79)
    return 0

def onoff80(TABs):
    if globals.enabled[80-1]:
        globals.enabled[80-1] = 0
        buttons.turnToGrayImages(TABs, 80)
    else:
        globals.enabled[80-1] = 1
        buttons.turnToRGBImages(TABs, 80)
    return 0

def onoff81(TABs):
    if globals.enabled[81-1]:
        globals.enabled[81-1] = 0
        buttons.turnToGrayImages(TABs, 81)
    else:
        globals.enabled[81-1] = 1
        buttons.turnToRGBImages(TABs, 81)
    return 0

def onoff82(TABs):
    if globals.enabled[82-1]:
        globals.enabled[82-1] = 0
        buttons.turnToGrayImages(TABs, 82)
    else:
        globals.enabled[82-1] = 1
        buttons.turnToRGBImages(TABs, 82)
    return 0

def onoff83(TABs):
    if globals.enabled[83-1]:
        globals.enabled[83-1] = 0
        buttons.turnToGrayImages(TABs, 83)
    else:
        globals.enabled[83-1] = 1
        buttons.turnToRGBImages(TABs, 83)
    return 0

def onoff84(TABs):
    if globals.enabled[84-1]:
        globals.enabled[84-1] = 0
        buttons.turnToGrayImages(TABs, 84)
    else:
        globals.enabled[84-1] = 1
        buttons.turnToRGBImages(TABs, 84)
    return 0

def onoff85(TABs):
    if globals.enabled[85-1]:
        globals.enabled[85-1] = 0
        buttons.turnToGrayImages(TABs, 85)
    else:
        globals.enabled[85-1] = 1
        buttons.turnToRGBImages(TABs, 85)
    return 0

def onoff86(TABs):
    if globals.enabled[86-1]:
        globals.enabled[86-1] = 0
        buttons.turnToGrayImages(TABs, 86)
    else:
        globals.enabled[86-1] = 1
        buttons.turnToRGBImages(TABs, 86)
    return 0

def onoff87(TABs):
    if globals.enabled[87-1]:
        globals.enabled[87-1] = 0
        buttons.turnToGrayImages(TABs, 87)
    else:
        globals.enabled[87-1] = 1
        buttons.turnToRGBImages(TABs, 87)
    return 0

def onoff88(TABs):
    if globals.enabled[88-1]:
        globals.enabled[88-1] = 0
        buttons.turnToGrayImages(TABs, 88)
    else:
        globals.enabled[88-1] = 1
        buttons.turnToRGBImages(TABs, 88)
    return 0

def onoff89(TABs):
    if globals.enabled[89-1]:
        globals.enabled[89-1] = 0
        buttons.turnToGrayImages(TABs, 89)
    else:
        globals.enabled[89-1] = 1
        buttons.turnToRGBImages(TABs, 89)
    return 0

def onoff90(TABs):
    if globals.enabled[90-1]:
        globals.enabled[90-1] = 0
        buttons.turnToGrayImages(TABs, 90)
    else:
        globals.enabled[90-1] = 1
        buttons.turnToRGBImages(TABs, 90)
    return 0

def onoff91(TABs):
    if globals.enabled[91-1]:
        globals.enabled[91-1] = 0
        buttons.turnToGrayImages(TABs, 91)
    else:
        globals.enabled[91-1] = 1
        buttons.turnToRGBImages(TABs, 91)
    return 0

def onoff92(TABs):
    if globals.enabled[92-1]:
        globals.enabled[92-1] = 0
        buttons.turnToGrayImages(TABs, 92)
    else:
        globals.enabled[92-1] = 1
        buttons.turnToRGBImages(TABs, 92)
    return 0

def onoff93(TABs):
    if globals.enabled[93-1]:
        globals.enabled[93-1] = 0
        buttons.turnToGrayImages(TABs, 93)
    else:
        globals.enabled[93-1] = 1
        buttons.turnToRGBImages(TABs, 93)
    return 0

def onoff94(TABs):
    if globals.enabled[94-1]:
        globals.enabled[94-1] = 0
        buttons.turnToGrayImages(TABs, 94)
    else:
        globals.enabled[94-1] = 1
        buttons.turnToRGBImages(TABs, 94)
    return 0

def onoff95(TABs):
    if globals.enabled[95-1]:
        globals.enabled[95-1] = 0
        buttons.turnToGrayImages(TABs, 95)
    else:
        globals.enabled[95-1] = 1
        buttons.turnToRGBImages(TABs, 95)
    return 0

def onoff96(TABs):
    if globals.enabled[96-1]:
        globals.enabled[96-1] = 0
        buttons.turnToGrayImages(TABs, 96)
    else:
        globals.enabled[96-1] = 1
        buttons.turnToRGBImages(TABs, 96)
    return 0

def onoff97(TABs):
    if globals.enabled[97-1]:
        globals.enabled[97-1] = 0
        buttons.turnToGrayImages(TABs, 97)
    else:
        globals.enabled[97-1] = 1
        buttons.turnToRGBImages(TABs, 97)
    return 0

def onoff98(TABs):
    if globals.enabled[98-1]:
        globals.enabled[98-1] = 0
        buttons.turnToGrayImages(TABs, 98)
    else:
        globals.enabled[98-1] = 1
        buttons.turnToRGBImages(TABs, 98)
    return 0

def onoff99(TABs):
    if globals.enabled[99-1]:
        globals.enabled[99-1] = 0
        buttons.turnToGrayImages(TABs, 99)
    else:
        globals.enabled[99-1] = 1
        buttons.turnToRGBImages(TABs, 99)
    return 0

def onoff100(TABs):
    if globals.enabled[100-1]:
        globals.enabled[100-1] = 0
        buttons.turnToGrayImages(TABs, 100)
    else:
        globals.enabled[100-1] = 1
        buttons.turnToRGBImages(TABs, 100)
    return 0

